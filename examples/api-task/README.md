# Example: API task

Validates an API task by running tests, checking a route exists, and verifying a local health endpoint.

Start the application first, then run:

```bash
donespec validate examples/api-task/done.json
```

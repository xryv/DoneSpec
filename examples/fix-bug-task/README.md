# Example: Fix bug task

Validates that tests pass, the expected auth file exists, `returnTo` was implemented, and shared types were not touched.

```bash
donespec validate examples/fix-bug-task/done.json
```
